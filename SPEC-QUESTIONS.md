# SPEC-QUESTIONS

`year_pillar` / `four_pillars` 的 v0.2 正式契约实现仍无待决问题。

业主于 2026-07-19 要求盲写大运、神煞及合盘关系，作为主实现对拍候选；但本仓
`contracts/` 仍钉在 v0.2.0，且 `paipan-spec.md` 4.5 明列“大运（v0.3）”为
“实现方不得先行猜测实现”。为不把未签署口径伪装成正式契约，候选实现被隔离在
`paipan_ref.blind_cli`，以下问题在纳入正式 `paipan_ref.cli` 和 CI 对拍前必须由
规格流程裁决：

1. 大运 op 的请求/响应 schema、性别枚举、默认运数，以及未知/不适用性别的错误语义。
2. “三日一岁”余数按日、时辰、小时或秒保留到何种精度；是否四舍五入；怎样把名义
   起运年龄映射到实际公历交运年月日。
3. 出生恰好等于节时，前一节/后一节的注入定义和顺逆两向应取零还是取下一完整节距。
4. 神煞正式范围与基准：年干/日干、年支/日支是否双查；文昌辛干采用“辛子”还是
   《古今图书集成》所见“辛戌”，或保留流派配置。
5. 刑按两支即报告，还是寅巳申/丑未戌必须三支齐全才激活；巳申等同时满足合与刑时
   是否保留全部关系。
6. 合盘十神只查双方显干，还是还要展开藏干；地支矩阵是否只做跨盘 4×4，是否需要
   同盘关系及柱位权重。
7. 这些 op 应进入 `paipan.schema.json`，还是建立独立 luck/compatibility schema。
